# --- AIOps Engine Configuration ---
# Prometheus URL to fetch metrics from
PROM_URL=https://prometheus.demo.do.prometheus.io

# Alertmanager URL to send anomalies to
ALERTMANAGER_URL=https://alertmanager.demo.do.prometheus.io

# Default Prometheus query to monitor
PROM_QUERY=up

# How many hours of historical data to use for training/detection
LOOKBACK_HOURS=24

# The expected proportion of outliers in the data (0.01 to 0.1)
CONTAMINATION=0.05

# --- Database Infrastructure ---
# Used by Docker to initialize the database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=aiops

# Connection string used by the Python application to connect to the database
# Format: postgresql://<user>:<password>@<host>:<port>/<db_name>
DATABASE_URL=postgresql://postgres:your_secure_password_here@postgres:5432/aiops
